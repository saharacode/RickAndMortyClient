package de.neuefische.rickandmortyclient.model;

import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;

@Data
@NoArgsConstructor
//@AllArgsConstructor
public class RMInfo {
    // class can stay empty because the content is not important for the servicemethods
    // maybe important to iterate over pages
}
